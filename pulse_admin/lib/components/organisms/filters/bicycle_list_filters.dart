import 'package:flutter/material.dart';

class BicycleListFilters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      children: [const Text("List filters")],
    );
  }
}
